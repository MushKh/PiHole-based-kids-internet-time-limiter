Instruction changes.

1. Manually run below commands to make sure they can change group status in PiHole.
sudo ./setGroupStatus.sh Kids_Group enable

Make sure sqlite3 installed. 
sudo apt install sqlite3

If setGroupStatus.sh bash file cannot be found, them most probably it was edited in Windows. 
TO fix run this command first.
sed -i 's/\r//g' setGroupStatus.sh




